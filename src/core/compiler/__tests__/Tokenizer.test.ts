describe('Tokenizer', () => {

    describe('Tokenization', () => {
        it('parses an opcode correctly', () => {

        });

        it('parses an immediate correctly', () => {

        });

        it('parses an address correctly', () => {

        });

        it('parses a jumplabel correctly', () => {

        });
        
        it('ignores comments', () => {
    
        });

        it('ignores whitespace', () => {

        });
    });

    describe('Error reporting', () => {
        it('logs an error when it finds an unidentified token', () => {
    
        });

        it('logs an error when an immediate is not specified after a #', () => {

        });

        it('logs an error when an address is not specified after a @', () => {

        });
    });

});