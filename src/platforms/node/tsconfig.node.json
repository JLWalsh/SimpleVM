{
    "extends": "../../../tsconfig.json",
    "include": [
        "./vm.node.ts"
    ],
    "compilerOptions": {
        "outDir": "../../../dist/node", /* Redirect output structure to the directory. */
    }
}